/target
/build
/data

# The live book project (author's content) stays local — never push it.
# The committed repo is the tool; samples/ is the MIT example.
/book.json
/chapters/
/en/
/product.json

# Studio/Shelf workspace (drafts + built products) — author content, local only.
/workspace/
/products/

# ECI FOGRA39 / ISOcoated drop-in (RB-46) — not shipped, set REBOOK_ICC_CMYK
/icc/*.icc

# Generated coloring plates (RB-51 `coloring-plates`)
/samples/coloring/plates/
# Print line-art rasters (coloring-book plates)
/samples/coloring/art/

# Book outputs — never track compiled/exported book artifacts
*.epub
*.azw3
*.kfx
*.mobi
*.txt
# Generated aggregate markdown export (currently rust_pered_sn_book.md)
*_book.md
_book.md
book_drafts/*.md